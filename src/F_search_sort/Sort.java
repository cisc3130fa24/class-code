package F_search_sort;

public class Sort {
    // at minimum: selection, merge, quick

    // if time: bubble (w/o and w/ optimization), insertion, bogo

    // double[], then generic T extends Comparable<T>, then with Comparator<? super T> -- can even do this with binary search!

    // TODO: research partition, see reading ?where?, maybe check out posted video

    // timing

    // with printing the array at each step (

    // slides for each sort: basic idea, tracing, whether in-place, whether stable, best and worst cases (if applicable)

    // post links to visualizations if find good ones
}
